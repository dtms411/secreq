# dependencies & build output, anywhere in the tree
node_modules/
.next/
*.tsbuildinfo

# secrets & local state
.env
.env.local
*.log
.backfill-journal.jsonl
.balances-journal.jsonl
# office-maintained entity→building map (copy from the .example, keep local)
deposit-recon/ocr/entitymap.json
