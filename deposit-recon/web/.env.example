# The deployment carries ONLY these public values. The service key never goes
# here — it stays on the CLI host that runs the backfill and ingest.
NEXT_PUBLIC_SUPABASE_URL=https://<investigation-project>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
